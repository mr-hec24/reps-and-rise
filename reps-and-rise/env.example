# =====================================================
# Expo Supabase Auth Starter - Environment Variables
# =====================================================
#
# Copy this file to `.env.local` and fill in your actual values
#
# 1. Go to your Supabase project dashboard
# 2. Navigate to Settings > API  
# 3. Copy your Project URL and anon public key
# 4. Replace the placeholder values below
#
# =====================================================

# Supabase Configuration (REQUIRED)
# =====================================================
# Your Supabase project URL (e.g., https://your-project.supabase.co)
EXPO_PUBLIC_SUPABASE_URL=your_supabase_project_url_here

# Your Supabase anon/public key (safe to expose in client-side code)
EXPO_PUBLIC_SUPABASE_ANON_KEY=your_supabase_anon_key_here

# Optional: Development Settings
# =====================================================

# Dark mode preference for development
# Options: 'light', 'dark', 'media' (follows system)
DARK_MODE=media

# Optional: Environment identifier
NODE_ENV=development

# =====================================================
# How to find your Supabase credentials:
# =====================================================
#
# 1. Go to https://supabase.com and sign in
# 2. Select your project (or create a new one)
# 3. Go to Settings (gear icon) > API
# 4. Find your Project URL and anon public key
# 5. Copy these values to your .env.local file
#
# Your .env.local file should look like:
#
# EXPO_PUBLIC_SUPABASE_URL=https://abcdefghijklmnop.supabase.co
# EXPO_PUBLIC_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...
#
# =====================================================

# =====================================================
# Security Notes:
# =====================================================
#
# - The EXPO_PUBLIC_ prefix makes these variables available 
#   to client-side code (they are not secret)
# - Never put your Supabase service_role key here 
#   (it should only be used server-side)
# - The .env.local file is ignored by git (see .gitignore)
# - Always use .env.local for your actual values, not .env
#
# ===================================================== 